# دليل استخدام المساعد الذكي — G.PACK 2.0

## المحتويات
1. [مقدمة](#مقدمة)
2. [كيفية الوصول](#كيفية-الوصول)
3. [الأوامر المتاحة](#الأوامر-المتاحة)
4. [أمثلة وسيناريوهات](#أمثلة-وسيناريوهات)
5. [الإجراءات التنفيذية](#الإجراءات-التنفيذية)
6. [نصائح متقدمة](#نصائح-متقدمة)

---

## مقدمة

المساعد الذكي في G.PACK 2.0 هو مساعد يعتمد على الذكاء الاصطناعي (AI) يعمل داخل النظام. يمكنه:

- **الاستعلام** عن البيانات (عملاء، مخزون، فواتير، موردين، مبيعات)
- **التحليل** واكتشاف الأنماط والمشكلات
- **اقتراح إجراءات** تنفيذية (إنشاء عميل، عرض سعر، دفعة، مهمة)
- **تنفيذ الإجراءات** مباشرة بعد موافقتك
- **توليد تقارير** ملخصة وتحليلية
- **الإدخال الصوتي** بالعربية

---

## كيفية الوصول

1. اضغط على أيقونة المساعد الذكي (🤖) في الشريط الجانبي
2. أو استخدم اختصار لوحة المفاتيح
3. يمكن استخدام **الميكروفون** للإدخال الصوتي بالعربية

---

## الأوامر المتاحة

### العملاء

| الأمر | الوصف | مثال |
|-------|-------|------|
| `getClientList` | قائمة العملاء | "اعرض قائمة العملاء" |
| `getClientDetails` | تفاصيل عميل | "معلومات عن عميل طابور" |
| `getClientProfitability` | ربحية عميل | "كم ربح عميل شاورما مريم؟" |
| `getInactiveClients` | عملاء غير نشطين | "ما هي العملاء الذين لم يطلبوا منذ شهر؟" |
| `getClientRiskAssessment` | تقييم مخاطر الائتمان | "قيّم مخاطر عميل سلطان الشواية" |

### المخزون

| الأمر | الوصف | مثال |
|-------|-------|------|
| `getInventoryStatus` | حالة المخزون | "أقل مخزون في مستودع طابور" |
| `getReorderSuggestions` | اقتراحات إعادة الطلب | "ما الذي يحتاج إعادة طلب؟" |
| `getStockMovement` | حركات المخزون | "حركات مخزون تيشيرت موظفين" |

> **ملاحظة:** يمكنك تحديد اسم المستودع في استعلام المخزون، مثل: "أقل مخزون في مستودع طابور"

### العروض والفواتير

| الأمر | الوصف | مثال |
|-------|-------|------|
| `getOrderStatus` | حالة الطلبات | "حالة الطلبات المعلقة" |
| `getSalesSummary` | ملخص المبيعات | "مبيعات اليوم" |
| `getProfitAnalysis` | تحليل الأرباح | "تحليل أرباح هذا الشهر" |
| `getOutstandingPayments` | دفعات مستحقة | "ما هي الفواتير غير المدفوعة؟" |

### الموردين

| الأمر | الوصف | مثال |
|-------|-------|------|
| `getSupplierPricing` | أسعار مورد | "أسعار مورد X لمنتج Y" |
| `compareSupplierPricing` | مقارنة أسعار | "قارن أسعار البوكس بين الموردين" |

### التحليلات الاستباقية

| الأمر | الوصف | مثال |
|-------|-------|------|
| `getProactiveAlerts` | تنبيهات شاملة | "ما هي التنبيهات الحالية؟" |
| `getRootCauseAnalysis` | تحليل سببي | "لماذا انخفضت مبيعات الشهر؟" |
| `getRecurringPatterns` | أنماط متكررة | "ما هي الطلبات المتكررة؟" |
| `getDiscountDecision` | قرار خصم | "هل أوافق على خصم 15% لعميل طابور؟" |

### التخطيط والمحاكاة

| الأمر | الوصف | مثال |
|-------|-------|------|
| `getBusinessPlanner` | خطة عمل | "ضع خطة عمل لزيادة المبيعات 20%" |
| `simulateAction` | محاكاة تأثير | "ماذا لو رفعت الأسعار 10%؟" |
| `getKPIStatus` | مؤشرات الأداء | "ما هي مؤشرات الأداء الحالية؟" |
| `getGoalStatus` | تتبع الأهداف | "كيف تقدم هدف مبيعات الشهر؟" |
| `getTimelineReplay` | إعادة عرض الأحداث | "ماذا حدث الأسبوع الماضي؟" |
| `getCompanyLearning` | تعلم الشركة | "ماذا تعلم النظام عن عملياتنا؟" |

### المهام

| الأمر | الوصف | مثال |
|-------|-------|------|
| `getTaskSummary` | ملخص المهام | "ما هي مهامي اليوم؟" |
| `getPendingTasks` | المهام المعلقة | "المهام المتأخرة" |

### تقارير

| الأمر | الوصف | مثال |
|-------|-------|------|
| `getDashboardData` | بيانات اللوحة | "ملخص لوحة التحكم" |
| `getAIMetrics` | مقاييس الـ AI | "كم سؤال تم طرح هذا الشهر؟" |

---

## أمثلة وسيناريوهات

### سيناريو 1: فحص مخزون مستودع معين

**السؤال:** "أقل مخزون في مستودع طابور"

**ما يحدث:**
1. الـ AI يستدعي `getInventoryStatus` مع `warehouse_name = "طابور"`
2. يرجع قائمة الأصناف التي مخزونها أقل من الحد المحدد
3. يعرض النتائج في جدول مع: الصنف، المقاس، المخزون، اسم المستودع
4. يقترح إجراءات: إنشاء طلبات شراء، إنشاء مهمة متابعة

### سيناريو 2: تحليل انخفاض المبيعات

**السؤال:** "لماذا انخفضت مبيعات الشهر؟"

**ما يحدث:**
1. الـ AI يستدعي `getRootCauseAnalysis`
2. يحلل: عدد الطلبات، متوسط قيمة الطلب، العملاء النشطين، المنتجات الأكثر تأثراً
3. يرجع تقرير بأهم الأسباب المحتملة
4. يقترح حلول عملية

### سيناريو 3: إنشاء عرض سعر لعميل

**السؤال:** "اعرض سعر لعميل طابور لـ 100 تيشيرت"

**ما يحدث:**
1. الـ AI يستدعي `getClientDetails` للتأكد من العميل
2. يستدعي `getProductCatalog` للمنتج
3. يقترح إجراء `create_quote` مع تفاصيل العرض
4. تضغط "تأكيد وتنفيذ" لإنشاء العرض فعلياً

### سيناريو 4: تقييم قرار خصم

**السؤال:** "هل أوافق على خصم 15% لعميل سلطان الشواية؟"

**ما يحدث:**
1. الـ AI يستدعي `getDiscountDecision`
2. يحلل: تاريخ العميل، حجم الطلبات، هامش الربح، تأثير الخصم
3. يرجع توصية: موافق / رفض / قابل للتفاوض مع الأسباب
4. يعرض الأرقام التفصيلية

### سيناريو 5: محاكاة تأثير تغيير الأسعار

**السؤال:** "ماذا لو رفعت أسعار البوكس 10%؟"

**ما يحدث:**
1. الـ AI يستدعي `simulateAction` مع `action_type = "price_change"`
2. يحلل: تأثير على المبيعات المتوقعة، الأرباح، رد فعل العملاء
3. يرجع تقرير بالأرقام المتوقعة قبل وبعد

### سيناريو 6: الملخص اليومي

**السؤال:** "ملخص اليوم"

**ما يحدث:**
1. الـ AI يستدعي `getDashboardData` و `getProactiveAlerts`
2. يعرض: مبيعات اليوم، طلبات معلقة، تنبيهات مخزون، دفعات مستحقة
3. يقترح إجراءات للأولويات

### سيناريو 7: البحث الشامل

**السؤال:** "بحث عن طابور"

**ما يحدث:**
1. الـ AI يستدعي `globalSearch`
2. يبحث في: العملاء، المنتجات، الطلبات، الفواتير، الموردين
3. يرجع نتائج مصنفة مع روابط تنقل

---

## الإجراءات التنفيذية

الإجراءات هي عمليات كتابية (تعدل البيانات). كل إجراء يمر بمرحلتين:

### مرحلة 1: الاقتراح (Propose)
الـ AI يعرض ملخص الإجراء للموافقة:
- نوع الإجراء
- التفاصيل (العميل، المبلغ، الأصناف...)
- القيمة الإجمالية

### مرحلة 2: التنفيذ (Execute)
بعد ضغط "تأكيد وتنفيذ":
- يتم تنفيذ الإجراء في قاعدة البيانات
- يتم تسجيل العملية في `ai_action_log`
- يتم تأكيد النتيجة

### الإجراءات المتاحة

| الإجراء | الوصف |
|---------|-------|
| `create_client` | إنشاء عميل جديد |
| `create_quote` | إنشاء عرض سعر |
| `add_payment` | تسجيل دفعة على فاتورة |
| `convert_quote_to_invoice` | تحويل عرض سعر إلى فاتورة |
| `create_task` | إنشاء مهمة جديدة |
| `bulk_create_reorders` | إنشاء أوامر شراء جماعية |

### التنفيذ الجماعي
عندما يقترح الـ AI عدة إجراءات، يمكنك:
- **تنفيذ الكل**: ينفذ جميع الإجراءات دفعة واحدة
- **تنفيذ فردي**: ينفذ إجراء واحد فقط
- **إلغاء**: يلغي جميع الاقتراحات

---

## نصائح متقدمة

### 1. كن محدداً
- ❌ "شوف المخزون" → نتائج عامة
- ✅ "أقل مخزون في مستودع طابور" → نتائج دقيقة

### 2. استخدم الأرقام
- ✅ "مبيعات آخر 30 يوم" → تقرير محدد
- ✅ "أعلى 5 عملاء بالأرباح" → قائمة مرتبة

### 3. اطلب تحليلات
- ✅ "حلل أسباب انخفاض أرباح شهر يوليو"
- ✅ "قارن مبيعات Q2 مع Q1"

### 4. استخدم الإدخال الصوتي
- اضغط أيقونة الميكروفون 🎤
- تكلم بالعربية بشكل طبيعي
- سيتم تحويل صوتك إلى نص تلقائياً

### 5. تقييم الاقتراحات
- كل رد من الـ AI يحتوي على أزرار 👍 / 👎
- التقييم يساعد الـ AI على التحسن المستمر

### 6. الأمان
- الـ AI **لا يمكنه** حذف بيانات (لا DELETE، لا DROP، لا TRUNCATE)
- جميع الإجراءات الكتابية تتطلب موافقتك الصريحة
- كل عملية يتم تسجيلها في سجل كامل

---

## ملاحظات تقنية

- المساعد الذكي يعمل عبر OpenAI API (gpt-4o-mini)
- يمكن تفعيل/تعطيل ميزات الـ AI عبر Feature Flags
- جميع استعلامات الـ AI تمر عبر نظام أمان (Safety Guard)
- البيانات الحساسة لا تُرسل خارج النظام
- يمكن تتبع أداء الـ AI عبر `/api/ai-assistant/metrics`

---

*G.PACK 2.0 — AI Assistant Guide | Version 2.0 | 2026*
